# experimental

調査ワークフローのサブエージェントが自動生成したファイル群。

**未レビュー。** 静的スキャンで秘密情報の混入と外部送信が無いことは確認済み
（通信先は tldv のホストのみ）だが、行単位のレビューはしていない。
動作も未検証。参考として残しているだけなので、使う前に自分で読むこと。

検証済みの本線は親ディレクトリの `console.js` と `tldv-grab`。
